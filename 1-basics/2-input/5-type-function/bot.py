#Test the type of different variables
first_value = 30.5
second_value = 5

print(first_value, "is of type", type(first_value))

print(first_value, "+", second_value, "is of type", type(first_value + second_value))
